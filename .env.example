# ══════════════════════════════════════════════
# POLYMARKET AI AGENT — Configuration
# Copy to .env and fill in your values
# ══════════════════════════════════════════════

# ── Claude API ──
CLAUDE_API_KEY=your-claude_api_key
CLAUDE_MODEL_HAIKU=your-claude_model_haiku
CLAUDE_MODEL_SONNET=your-claude_model_sonnet

GEMINI_API_KEY=your-gemini_api_key

# ── Trading Parameters ──
INITIAL_BALANCE=your-initial_balance
MAX_POSITION_PCT=your-max_position_pct
MIN_EDGE_THRESHOLD=your-min_edge_threshold
KILL_THRESHOLD=your-kill_threshold
KELLY_FRACTION=your-kelly_fraction

# ── Agent Loop ──
SCAN_INTERVAL_SECS=your-scan_interval_secs
MAX_MARKETS_SCAN=your-max_markets_scan
MAX_HAIKU_PER_CYCLE=your-max_haiku_per_cycle
MAX_SONNET_PER_CYCLE=your-max_sonnet_per_cycle

# ── Polymarket API ──
GAMMA_API=your-gamma_api
POLYMARKET_CLOB_API=your-polymarket_clob_api
POLYMARKET_HOST=your-polymarket_host

# ── Wallet (required for live trading) ──
WALLET_PRIVATE_KEY=your-wallet_private_key

# ── Email Alerts (Gmail example) ──
# For Gmail: enable 2FA, create App Password at myaccount.google.com
MAIL_MAILER=your-mail_mailer
SMTP_HOST=your-smtp_host
SMTP_PORT=your-smtp_port
SMTP_USER=your-smtp_user
SMTP_PASS=your-smtp_pass
MAIL_ENCRYPTION=your-mail_encryption
ALERT_FROM="your-alert_from"
ALERT_TO=your-alert_to

# ── Telegram Alerts ──
TELEGRAM_BOT_TOKEN=your-telegram_bot_token
TELEGRAM_CHAT_ID=your-telegram_chat_id

# ── State ──
DB_PATH=your-db_path

# ── Mode ──
PAPER_TRADING=your-paper_trading
SIMULATE_AI=your-simulate_ai

# ── Logging ──
RUST_LOG=your-rust_log

# ── Agent Mode ──
GEMINI_ONLY=your-gemini_only

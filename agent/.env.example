# ══════════════════════════════════════════════
# POLYMARKET AI TRADING AGENT — Configuration
# Copy to .env and fill in your values
# ══════════════════════════════════════════════

# ═══ REQUIRED — AI API Keys ═══
# Get Gemini key (free): https://ai.google.dev
GEMINI_API_KEY=
# Claude/Sonnet is DISABLED for cost optimization (Gemini-only mode)
# CLAUDE_API_KEY=

# ═══ MODE ═══
# paper = virtual money, real data (DEFAULT — recommended for new users)
# live  = real money on Polymarket (requires wallet setup)
TRADING_MODE=paper

# ═══ KNOWLEDGE SHARING (opt-in) ═══
# Share anonymous trade data with community? (true/false)
# In return, your bot gets smarter from community experience
# SHARED: win/loss, category, trade mode, AI confidence
# NEVER:  wallet, balance, identity, specific markets
KNOWLEDGE_SHARING=true

# ═══ TELEGRAM ALERTS (optional, recommended) ═══
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ═══ EMAIL REPORTS (optional) ═══
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
ALERT_FROM=
ALERT_TO=

# ═══ LIVE MODE ONLY ═══
# WARNING: Only fill these if TRADING_MODE=live
# NEVER share your private key with anyone!
WALLET_PRIVATE_KEY=
POLY_API_KEY=
POLY_SECRET=
POLY_PASSPHRASE=

# ═══ POLYMARKET API ═══
GAMMA_API=https://gamma-api.polymarket.com
POLYMARKET_CLOB_API=https://clob.polymarket.com
POLYMARKET_HOST=https://polymarket.com

# ═══ TRADING PARAMETERS (defaults are good) ═══
INITIAL_BALANCE=100.0
SCAN_INTERVAL_SECS=3600
MAX_MARKETS_SCAN=700
MAX_CANDIDATES=20
MAX_DEEP_ANALYSIS=10
MIN_CONFIDENCE=0.50
MIN_EDGE_THRESHOLD=0.07
MAX_POSITION_PCT=0.10
KELLY_FRACTION=0.40
KILL_THRESHOLD=3.00
CATEGORY_FILTER=all

# ═══ PAPER TRADING ═══
MAX_OPEN_POSITIONS=8
REPORT_INTERVAL_HOURS=12
MAX_SPREAD=0.05
BALANCE_RESERVE_PCT=0.10
PRICE_CHECK_SECS=180

# ═══ STATE ═══
DB_PATH=agent.db

# ═══ LOGGING ═══
RUST_LOG=info
